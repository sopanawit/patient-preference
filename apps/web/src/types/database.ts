// ============================================================================
// Supabase generated types — PLACEHOLDER
//
// อย่าแก้ไฟล์นี้ด้วยมือ ให้ generate จาก schema จริงด้วย:
//   bun run gen:types
// (ต้องรัน `supabase start` + apply migrations ก่อน — ดู CLAUDE.md ข้อ 10)
//
// ระหว่าง Phase 0 ยังไม่มีการ generate จึงประกาศ type ว่างไว้ให้ build ผ่าน
// ============================================================================

export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
